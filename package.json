{
  "name": "piggogest",
  "version": "1.0.0",
  "description": "Piggogest monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel --recursive dev",
    "build": "pnpm run --recursive build",
    "test": "pnpm run --recursive test",
    "lint": "pnpm run --recursive lint",
    "clean": "pnpm run --recursive clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}